# 三狗珊瑚（ThreeDogCoral）开发日志

> 作者：@Ne
> 一个"越想越离谱"的记忆缓存系统的完整轨迹：从 ComfyUI 提示词管理 + RAG 检查工具的想法，
> 到带热度生命周期、融合检索、可热加载配置的 Agent 记忆层。

---

## 起源（2026-08-06）

> "我是 2026 年 8 月 6 日有的想法，那时候我还在折腾 ComfyUI 的提示词管理和 RAG，
> 后面越想越离谱就变成这样了。" —— 作者

最初想做的是 **RAG 检查工具**（查重复 chunk、陈旧内容、质量），后来发现从里面
"派生出了一个新东西"：一个**带热度生命周期的持久化记忆层中间件**。
它既能当 RAG 质检器用（去重/陈旧/重要性排序），又能当 Agent 的长期记忆。
于是三狗珊瑚诞生。

---

## 时间线

### 2026-08-06 · 起源
- 在 ComfyUI 提示词管理 + RAG 的方向上折腾，萌生"记忆缓存系统"的想法。

### 重构：ThreeDogMemory → ThreeDogCoral
- 保留原三级存储（热/温/冷）骨架与内部命名（`_evict_check` / `_dump_cold` / `_trim_cold_storage` / `_search_cold` / `_save_warm`）；
- **向量化**：每条记忆生成 384 维嵌入（本地 sentence-transformers/all-MiniLM-L6-v2），
  矩阵存独立 `.npy`（+ id 索引 JSON），与文本并行维护；环境无模型时自动降级为确定性哈希嵌入；
- **多路融合检索**：向量余弦 0.6 + 关键词 Jaccard 0.2 + 时间衰减 0.2（τ 默认 7 天），返回 Top-K 带分项得分；
- **热度淘汰**：H = 频率 0.4 + 最近访问 0.3 + 重要性 0.3；超容量先蒸馏（占位接口）再淘汰最低热度；
- **配置热加载**：所有阈值来自 `coral_config.json`，mtime 检测自动重载，无需重启；
- **Harness 工具注册**：`@register_tool` 注册 `memory_search` / `memory_insert`，
  附带 `build_dsh_cordis_plugin_js()`（DSH 原生 `harness.registerTool` 格式）与 `MemoryToolSidecar` 桥接。

### 200 轮对话压测（翻译软件助手场景）
- 关键词钩子（偏好/风格/术语…）命中且距上次分析 ≥ 20 轮对话 → 触发"翻译画像分析"；
- 结果：9 次画像分析，相邻间隔全部 21（≥ 20 冷却期）；容量治理 57 次淘汰后 total 精确收敛；
- 画像随对话演进（v1 两字段 → v9 七字段），重申偏好合并去重并获得淘汰保护。

### 跨项目共享基准
- 隔离 / 共享池 / 共享+项目亲缘度重排 三配置对照：
  - 共享池：recall 不掉但 precision 1.0 → 0.77（跨域噪声挤占 Top-5）；
  - 共享+亲缘度（同项目/用户级记忆 +0.15）：precision 回到 1.0；
  - **冷启动**：新项目隔离 0% vs 共享 100% —— 跨项目最大的收益来源；
- 结论：最优形态是"共享池 + project 字段 + 亲缘度 + 每项目配额"。

### 磁盘 ↔ 命中率权衡曲线（用户想法：用硬盘换上下文/命中率）
- 容量 50→400：precision 0.73→1.0，磁盘 3KB→63KB；
- 发现**中间规模池干扰**现象：容量 100 左右精度反而最低（半相关噪声挤占）；
- 每条记忆约 1.8KB（文本 ~250B + 向量 1536B），10 万条 ≈ 180MB —— 硬盘确实"便宜到可以随便换"。

### 并行化（用户约束：2020 年前后消费级 i5/i7，6C/12T~8C/16T）
- **嵌入合批**：batch 1→64 吞吐 3.9×；8 路并发检索 + 8ms 合批窗口 = 5.9× 加速（8 并发 21.8ms vs 串行 128ms）；
- **位图 Jaccard**（numpy `bitwise_count`）：稳态 20× 加速（20000 条 138ms vs 2834ms）；
- 端到端检索 2000 条池 4ms/查询；
- 教训：**"把 numpy 调用套进 Python 循环"比旧代码还慢 5×** —— 批量化必须整池一次；
  合批窗口对串行流量是纯税（默认 0，真实模型才开 4-8ms）。

### 热度系统改造
- 修复 **冷库热度不落盘**：检索命中冷库记忆只改内存（对象每次重新加载，改完即丢）→
  改为待合并增量 + 各重写路径/`flush()`/节流折叠写回（验证：1→命中→重启后 2）；
- 频率改对数刻度 `log2(1+c)/log2(1+scale)`，淘汰/蒸馏时按池最大访问数归一化（跨池可比）。

### 20000 次暴力压测（鲁棒性）
- **无致命 bug**：全程零未捕获异常、统计一致（唯一内容 == 向量数）、治理精确收敛、重启一致；
- 但命中三个 **O(n²) 规模反噬**（"1 千条全对，2 万条塌方"）：
  1. 向量存储每次 insert 全量 `np.save`（2 万条写入 472.8s，写盘 ~300GB）；
  2. `_search_cold` 用 `readlines()` 全读冷库再切片（30ms/次，`cold_scan_lines` 配置形同虚设）；
  3. 超容后每次 insert 全量治理（1026ms/次）；
  - 外加：`stats()` 每次全量数冷库行（21ms/次）；`load_config` 对坏路径**静默回退默认配置**
    （差点把数据写进错误目录 —— 传错路径只打一行 warning）。

### P0/P1 修复（本次会话收官）
- `_VectorStore` 几何扩容（amortized O(1) 追加）+ 只写有效行 + 原地压缩；
- 向量落盘节流（默认 5s，`flush()` 恒落盘）；
- 冷库**尾部流式读**（向后分块，只读最后 64KB×N）；
- **治理余量**（headroom，默认 `max(10, min(容量/10, 200))`）—— 超容后批量淘汰而非每次全量；
- `stats()` 改用缓存计数（21ms → µs）；
- **启动时清理孤儿向量**（热区不落盘，重启后其向量无主，逐次累积）。

**2 万次压测 修复前 vs 修复后（同机同场景）：**

| 环节 | 修复前 | 修复后 | 提升 |
|---|---|---|---|
| 写入 2 万条 | 472.8s | **88.8s**（4.4ms/条） | 5.3× |
| 检索 | 30ms/次 | 13ms/次 | 2.3× |
| `stats()` | 21ms/次 | ~0ms | — |
| 超容后插 300 条（治理） | 307.8s | **3.4s** | 90× |
| 完整性/重启 | OK / 不一致 | **全部通过 ✅** | — |

### 存储配额（用户想法：截断点/警告）
- `storage.max_bytes` 硬线 + `warn_ratio` 警告线 + `hard_ratio` 回落线；
- 向量字节用**投影值**（节流落盘下读磁盘文件会低估 600KB+）；
- 行为：超警告线 → 告警一次；超硬线 → 按热度淘汰冷库，回落到振荡带 [hard, max]；
- 配额的语义是"保护最终要写盘的量"，振荡带是刻意设计（触发点 ~max，回落点 hard）。

---

### 打包分享准备（2026-08-14 收官）
- 仓库化：`pyproject.toml`（包名 `coral-memory`）、`README.md`（定位/特性/配置/基准/适用边界）、
  `LICENSE`（MIT）、`.gitignore`；
- 目录整理：`tests/`（200 轮回归）、`benchmarks/`（跨项目/磁盘/并行）、`stress/`（暴力压测）、
  `examples/`（使用示例 + `llm_distill.py` 真实 LLM 蒸馏接入示例）；
- 全部脚本从子目录直接运行验证通过（sys.path 引导）。

## 用户提出的关键想法（本项目的方向来源）

1. **用硬盘空间换上下文/命中率** → 容量-命中率曲线、存储配额；
2. **异步数量可以提升（2020 i5/i7）** → 嵌入合批、位图向量化、锁外嵌入、治理余量；
3. **存储超容的截断点/警告** → `storage` 配额体系。

## 未来方向（待办）

- **归档压缩层**：热/温/冷/归档 四级，归档按 1MB 段 gzip —— "用 CPU 换磁盘"，比删除温和的配额出口；
- **向量量化**：float32 → int8（4× 空间，recall 损失 ~1-2%）—— 配额告急时的有损旋钮；
- **配额增长预测**：`disk_usage()` 加近 N 分钟增速 + 预计触顶时间（预警 + 建议，而非只告警）；
- **`_distill` 接入真实 LLM**（当前占位返回 None，聚类已按占位短路）；
- **RAG 质检模式**：重复簇 / 陈旧 chunk / 热度分布审计报告（回到最初的构想）；
- **打包分享**：README + LICENSE、bench 收编 `tests/`、清理工作区、定包名（`coral-memory`）。

## 教训清单

1. **O(n²) 反噬**：1 千条全对的代码，2 万条会塌方 —— 每次写全量文件的"简单实现"最危险；
2. **numpy 批量 ≠ 循环里调 numpy**：2000 次小调用比纯 Python 还慢 5×；
3. **合批窗口是税**：对串行流量默认必须为 0；
4. **静默回退是地雷**：配置加载失败应抛异常，而不是悄悄换存储；
5. **度量优先于猜测**：热度/配额/并发的每个结论都有对应基准（`bench_*.py` / `stress_20k.py`）。
